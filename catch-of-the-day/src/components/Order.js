import React from 'react';

// 1. convert to FC
// 2. use fishes, order props to derive total, display items
// 3. render items with availability in mind: {count} lbs {name}\n{price + count}

class Order extends React.Component {
  render() {
    return <div className="order">Order!!!</div>;
  }
}

export default Order;
